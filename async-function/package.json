{
  "name": "incident-senpai-async-function",
  "version": "2.0.0",
  "scripts": {
    "unit": "jest",
    "lint": "eslint '*.ts' --quiet --fix",
    "compile": "tsc",
    "test": "npm run compile && npm run unit"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.598.0",
    "@slack/web-api": "^7.1.0",
    "date-fns": "^3.6.0",
    "yaml": "^2.4.5",
    "esbuild": "^0.21.5"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.1",
    "@types/aws-lambda": "^8.10.138",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}